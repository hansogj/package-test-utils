{
  "name": "package-test-utils-javascript",
  "author": "@hansogj",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && mkdir dist && cp ../../node_modules/@hansogj node_modules/shared index.html style.css src/ dist/. -R",
    "preserve": "pnpm build",
    "serve": "http-server -p 2112 dist ",
    "test": "echo \"Error: no test specified\""
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}